#pragma once

/* Open/close the pre-fx overlay (tuner + noise gate tabs). */
void ui_pre_fx_open(void);
void ui_pre_fx_close(void);

/* True if the overlay is currently visible. */
int  ui_pre_fx_is_open(void);
