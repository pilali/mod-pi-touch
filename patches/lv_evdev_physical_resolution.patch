--- a/src/drivers/evdev/lv_evdev.c
+++ b/src/drivers/evdev/lv_evdev.c
@@ -100,8 +100,11 @@ static lv_point_t _evdev_process_pointer(lv_indev_t * indev, int x, int y)
     int offset_x = lv_display_get_offset_x(disp);
     int offset_y = lv_display_get_offset_y(disp);
-    int width = lv_display_get_horizontal_resolution(disp);
-    int height = lv_display_get_vertical_resolution(disp);
+    /* Use physical (pre-rotation) resolution so that evdev outputs coordinates
+     * in physical display space. LVGL's indev rotation transform (lv_indev.c)
+     * then maps them to the logical (post-rotation) coordinate space.
+     * Using logical resolution here would cause a double transformation. */
+    int width = lv_display_get_physical_horizontal_resolution(disp);
+    int height = lv_display_get_physical_vertical_resolution(disp);

     lv_point_t p;
